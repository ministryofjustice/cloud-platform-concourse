resource_types:
- name: pull-request
  type: docker-image
  source:
    repository: teliaoss/github-pr-resource

resources:
- name: cloud-platform-environments-david-test1-pull-requests
  type: pull-request
  check_every: 1m
  source:
    repository: digitalronin/cloud-platform-environments
    access_token: ((digitalronin-environments-pr-git-access-token))
    paths:
    - namespaces/david-test1.cloud-platform.service.justice.gov.uk
- name: tools-image
  type: docker-image
  source:
    repository: registry.hub.docker.com/ministryofjustice/cloud-platform-tools
    tag: 1.5

groups:
- name: david-test1
  jobs: [plan-david-test1]

jobs:
  - name: plan-david-test1
    serial: true
    plan:
      - get: cloud-platform-environments-david-test1-pull-requests
        trigger: true
        version: every
      - put: cloud-platform-environments-david-test1-pull-requests
        params:
          path: cloud-platform-environments-david-test1-pull-requests
          status: pending
      - get: tools-image
      - task: plan-environments
        image: tools-image
        config:
          platform: linux
          inputs:
            - name: cloud-platform-environments-david-test1-pull-requests
          params:
            AWS_ACCESS_KEY_ID: ((aws-live-1.access-key-id))
            AWS_SECRET_ACCESS_KEY: ((aws-live-1.secret-access-key))
            KUBECONFIG_AWS_ACCESS_KEY_ID: ((aws-live-1.access-key-id))
            KUBECONFIG_AWS_SECRET_ACCESS_KEY: ((aws-live-1.secret-access-key))
            KUBECONFIG: /tmp/kubeconfig
            TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
            # the variables prefixed with PIPELINE_ are used by the plan script
            PIPELINE_CLUSTER: david-test1.cloud-platform.service.justice.gov.uk
            PIPELINE_STATE_BUCKET: cloud-platform-terraform-state
            PIPELINE_STATE_KEY_PREFIX: "cloud-platform-environments/"
            PIPELINE_STATE_REGION: "eu-west-1"
            PIPELINE_CLUSTER_STATE_BUCKET: cloud-platform-terraform-state
            PIPELINE_CLUSTER_STATE_KEY_PREFIX: "cloud-platform/"
            PIPELINE_TERRAFORM_STATE_LOCK_TABLE: "cloud-platform-environments-terraform-lock"
          run:
            path: /bin/sh
            dir: cloud-platform-environments-david-test1-pull-requests
            args:
              - -c
              - |
                mkdir -p "${TF_PLUGIN_CACHE_DIR}"
                (
                  AWS_ACCESS_KEY_ID="${KUBECONFIG_AWS_ACCESS_KEY_ID}"
                  AWS_SECRET_ACCESS_KEY="${KUBECONFIG_AWS_SECRET_ACCESS_KEY}"
                  aws s3 cp s3://cloud-platform-concourse-kubeconfig/kubeconfig /tmp/kubeconfig
                )
                export branch_head_sha=$(cat .git/resource/metadata.json | jq -r '.[] | select(.name == "head_sha") | .value')
                export master_base_sha=$(cat .git/resource/metadata.json | jq -r '.[] | select(.name == "base_sha") | .value')
                ./bin/plan
        on_failure:
            put: cloud-platform-environments-david-test1-pull-requests
            params:
              path: cloud-platform-environments-david-test1-pull-requests
              status: failure
        on_success:
            put: cloud-platform-environments-david-test1-pull-requests
            params:
              path: cloud-platform-environments-david-test1-pull-requests
              status: success
